class Obstacles {
  
  // Top left corner is where the drawing starts,
  // but it draws to bottom right, which is the 
  // ending position.
  PVector startingPos;
  PVector endingPos;
  
  Obstacles(PVector start) {
    startingPos = start;
  }
  
  void animatePlacing() {

  }
}
